export function Footer() {
  return (
    <footer className="border-t border-line bg-paper-dim">
      <div className="max-w-6xl mx-auto px-5 sm:px-8 py-6">
        <p className="text-sm text-ink-faint max-w-3xl">
          <span className="font-semibold text-ink-soft">Prototype:</span> SignBridge demonstrates a
          controlled learning workflow around one real lesson. Signed content uses genuine Auslan-derived
          motion for a small vocabulary, but has not been validated by a fluent Auslan signer, and is not
          a replacement for human Auslan interpreters. See &ldquo;About this prototype&rdquo; for details.
        </p>
      </div>
    </footer>
  );
}
